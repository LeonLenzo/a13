# Kallisto quantification output (large raw data — keep run_info.json)
03_kallisto/kallisto_quant/**/abundance.h5
03_kallisto/kallisto_quant/**/abundance.tsv

# DESeq2 binary objects and large result tables (regenerable from scripts)
*.rds
04_deseq2/results/deseq_results_*.csv
04_deseq2/results/gene_list_*.csv

# Large reference files (download instructions in scripts)
reference/mart_export.txt
reference/ncbi_to_ensembl.tsv
reference/*.fna
reference/*.fna.gz
reference/*.idx

# Raw reads
reads/
bbrepair_stats/*.stats.csv
